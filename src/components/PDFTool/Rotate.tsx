import React, { PropsWithChildren } from 'react'

interface RotateProps {
  file: File
  totalPages: number
  loading?: boolean
}
function Rotate({}: PropsWithChildren<RotateProps>) {
  return <div></div>
}
export default Rotate
